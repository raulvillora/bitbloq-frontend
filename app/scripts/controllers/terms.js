'use strict';

/**
 * @ngdoc function
 * @name bitbloqApp.controller:TermsCtrl
 * @description
 * # TermsCtrl
 * Controller of the bitbloqApp
 */
angular.module('bitbloqApp')
    .controller('TermsCtrl', function($scope, $translate) {
        $scope.translate = $translate;
    });
